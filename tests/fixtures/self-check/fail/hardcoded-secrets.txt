diff --git a/src/services/payment.ts b/src/services/payment.ts
index 1234567..abcdef0 100644
--- a/src/services/payment.ts
+++ b/src/services/payment.ts
@@ -1,5 +1,12 @@
 import { PaymentGateway } from './gateway';

+const STRIPE_API_KEY = "sk_live_EXAMPLE_FAKE_KEY_DO_NOT_USE";
+const STRIPE_SECRET = "whsec_EXAMPLE_FAKE_SECRET_DO_NOT_USE";
+
 export class PaymentService {
-  constructor(private gateway: PaymentGateway) {}
+  constructor(private gateway: PaymentGateway) {
+    this.gateway.configure({
+      apiKey: STRIPE_API_KEY,
+      webhookSecret: STRIPE_SECRET,
+    });
+  }

+  async processPayment(amount: number) {
+    const result = await this.gateway.charge(amount);
+    return result;
+  }
 }
